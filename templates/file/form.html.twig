{% extends 'base.html.twig' %}

{% block title %}Charger un fichier |{{parent() }}{% endblock %}

{% block body %}
<a href="{{path('main')}}" class="btn btn-outline-secondary d-inline-block mt-2">Retour à l'accueil</a>
    <section class="d-flex w-50 mx-auto my-auto content-container">
        <figure class="w-50">
            <img src="{{asset('images/vault.jpg')}}" class="w-100" alt="">
        </figure>
        <article class="d-flex flex-column justify-content-center w-50">
             <h1 class="text-center">Charger un fichier </h1>
              {{ form(form, {'attr':{'novalidate':'novalidate', 'class':'mx-auto text-center'}} ) }}
        </article>
       
    </section>

{% endblock %}
